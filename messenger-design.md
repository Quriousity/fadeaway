# 임시성 메신저 플랫폼 — 설계 문서

> **문서 버전**: v0.1 (초안)
> **최종 수정**: 2026-08-09
> **상태**: 설계 단계 / 구현 미착수

---

## 0. 이 문서 사용법 (AI 협업 규약)

이 문서는 AI와 함께 반복적으로 다듬는 것을 전제로 작성됨. 아래 규약을 지킬 것.

### 0.1 태그 체계

각 항목은 다음 상태 중 하나를 가진다.

| 태그 | 의미 | AI의 취급 방식 |
|---|---|---|
| `[FIXED]` | 확정. 제품 정체성에 직결됨 | 변경 제안 금지. 이것과 충돌하는 설계는 폐기 |
| `[DECIDED]` | 결정됨. 근거 있음 | 더 나은 근거가 있으면 반박 가능. 근거 없이 바꾸지 말 것 |
| `[OPEN]` | 미결정 | 자유롭게 제안. 선택지와 트레이드오프를 함께 제시할 것 |
| `[UNKNOWN]` | 숫자·사실 미확인 | 추측으로 채우지 말 것. "측정 필요"로 남길 것 |
| `[RISK]` | 알려진 위험 | 해결됐다고 임의로 판단하지 말 것 |

### 0.2 AI에게 요구하는 태도

- **추측한 숫자를 사실처럼 쓰지 말 것.** 모르면 `[UNKNOWN]`으로 남긴다.
- **`[FIXED]`와 충돌하는 설계는 아무리 효율적이어도 제안하지 않는다.**
- 새 결정을 추가할 때는 §2 결정 기록에 근거와 함께 append 한다. 기존 항목을 조용히 수정하지 않는다.
- 이 문서는 **1인 개발자 + 필요 시 최소 인원** 체제를 전제로 한다. 운영 부담이 큰 제안은 그 비용을 명시할 것.

---

## 1. 제품 정의

### 1.1 한 줄 정의 `[FIXED]`

**데이터가 누적되지 않는 메신저.** 채팅·음성·영상 통화를 제공하되, 모든 데이터는 짧은 수명을 가지고 자동 소멸한다.

### 1.2 핵심 전제 `[FIXED]`

이 제품의 유일하고 복제 불가능한 강점은 **리스크 총량이 운영 기간에 비례하지 않는다**는 구조다.

- 대형 메신저: 10년 운영 → 10년치 데이터 보유 → 사고 시 10년치 노출
- 이 제품: 10년 운영 → 항상 최근 2주치만 보유 → 사고 시에도 노출 범위 상한이 고정

대형 플랫폼이 이 구조를 못 만드는 이유는 기술이 아니라 **데이터가 그들의 수익원이기 때문**이다. 따라서 이 포지션은 구조적으로 비어 있고, 자금력으로 밀리는 상황에서도 방어 가능하다.

**→ 이 전제를 훼손하는 모든 기능 추가는 거부한다.** (데이터 분석 기반 추천, 행동 로그 축적, 대화 기반 광고 타겟팅 등)

### 1.3 타깃 진입 시나리오 `[DECIDED]`

네트워크 효과를 정면 돌파하지 않는다. "친구가 안 써서 못 옮긴다"는 벽은 기능으로 못 넘는다.

대신 **일회성·목적성 사용**을 진입점으로 삼는다.

- "잠깐 계정 만들어서 통화하자" 가 자연스러운 상황
- 예: 중고거래 통화, 일회성 상담, 화상 면접, 단기 프로젝트 협업, 기록이 남으면 곤란한 대화

**설계 함의**: 가입 마찰이 극도로 낮아야 한다. 이 시나리오에서 회원가입 3분은 곧 이탈이다.

### 1.4 명시적 비목표 `[FIXED]`

- 전 국민 메신저를 목표하지 않는다
- 카카오톡 대체를 목표하지 않는다
- 데이터 기반 수익화를 하지 않는다
- 소셜 그래프(친구 목록, 피드, 프로필 탐색)를 키우지 않는다

---

## 2. 결정 기록 (Decision Log)

> 새 결정은 여기에 append. 번호는 재사용하지 않는다.

### D-001. 채팅 보관 기간 2주 `[FIXED]`

메시지는 생성 후 14일에 삭제된다. 예외 없음.

**함의**: "삭제"는 primary DB에서 지우는 것으로 끝나지 않는다. §4.3 참조.

### D-002. 통화 녹화는 전원 동의 옵트인 `[DECIDED]`

당초 "기본 녹화" 구상에서 변경.

**근거**: 통신비밀보호법상 플랫폼은 대화 당사자가 아니다. 전원 동의 없는 서버 측 녹화는 감청 소지가 있다. 음성·영상은 개인정보보호법상 민감도가 높다.

**요구사항**:
- 통화 시작 시 참가자 **전원** 명시적 동의
- 녹화 중 상시 시각 표시 (모든 참가자 화면)
- 참가자 1인이라도 거부 시 녹화 불가
- 동의 이력은 조작 불가능한 로그로 보존 (§8.2)

`[RISK]` 이 동의 절차가 "전화"를 "회의 예약"처럼 만들 수 있음. UX 마찰 측정 필요.

### D-003. 녹화본 보관 30분 `[DECIDED]`

통화 종료 직후 30분간만 다운로드 가능. 이후 하드 삭제.

`[OPEN]` 30분이 적절한가? 사용자가 통화 직후 자리를 뜨는 경우가 흔하다면 실효성이 낮다. 대안: 24시간 / 사용자 선택(최대 N시간).

### D-004. 로컬 녹화와 서버 녹화 병행 제공 `[OPEN]`

- **로컬 녹화**: 클라이언트에서 MediaRecorder로 저장. 서버 원가 0, 법적 리스크 최소. 단 모바일 브라우저 제약 큼
- **서버 녹화**: SFU에서 컴포지팅. 품질·안정성 우수. 원가와 리스크 모두 높음

`[OPEN]` 둘 다 할지, 하나만 최적화할지 미정. **1인 운영 관점에서는 로컬 우선이 유리.**

### D-005. AI 대화방은 E2EE 대상에서 제외 `[FIXED]`

AI가 메시지를 읽으려면 서버가 평문을 봐야 한다. E2EE와 AI 기능은 원리적으로 양립 불가.

**따라서 처음부터 분리 설계한다**:
- 일반 대화방: E2EE 적용 (또는 적용 로드맵)
- AI 활성 대화방: E2EE 아님. UI에서 명확히 구분 표시

나중에 붙이려 하면 스키마를 전부 재설계해야 하므로, **1단계부터 방 타입을 분리**할 것.

### D-006. 수익 모델은 크레딧 단일 `[DECIDED]`

**크레딧(선불/종량) = 사실상 유일한 수익원.**

광고는 수익 모델에서 **0으로 계산한다**. 근거는 §7.3.

### D-007. 광고는 직접 선별 방식, 애드 네트워크 미사용 `[DECIDED]`

- 지인 업체 / 공익 단체 중 직접 선별
- 서드파티 광고 SDK(AdSense, Meta 픽셀 등) **사용 금지**

**근거**: 애드 네트워크를 붙이는 순간 대화 내용을 안 넘겨도 기기 식별자·행동 로그가 외부로 나간다. "데이터를 안 쓴다"는 주장과 직접 충돌하며, 이 하나로 포지셔닝 전체가 무너진다.

**운영 제약**: 직접 판매는 계약·소재 검수·정산이 전부 수동. 1인 운영에서 시간을 먹으므로 **슬롯 수를 초기에 고정**할 것. 게재 기준과 중단 조건을 사전 문서화 (지인 업체는 내리기가 가장 어려운 상대).

### D-008. 외부 감사업체 도입 보류 `[DECIDED]`

당초 "DB 내용까지 감사하는 인력 확보" 구상에서 변경.

**근거 1**: 개인정보보호법상 책임은 개인정보처리자(= 플랫폼)에게 귀속되며 계약으로 외주화되지 않는다. 감사업체와의 계약은 두 회사 간 구상권 문제일 뿐, 규제기관 과징금과 사용자 집단소송은 전부 플랫폼을 향한다.

**근거 2**: 감사 비용을 피감사자가 지불하는 구조는 근본적 이해상충을 가진다 (엔론-아서앤더슨, 2008 신용평가사). "돈이 되니 정직할 것"은 성립하지 않는다.

**근거 3**: 사람이 실제 대화 내용을 열람하는 순간 프라이버시 포지셔닝이 무너지고, 감사자 자신이 새로운 유출 경로가 된다.

**단, 감사 로그 구조 자체는 유지한다.** §8.2 참조. 빼는 것은 "책임을 나눠 갖는 제3자"이지 "증거를 만드는 구조"가 아니다.

### D-009. 법적 방어선은 동의서가 아닌 기술적 안전조치 `[FIXED]`

**"사용자가 동의했다"는 방어가 되지 않는다.** 동의는 수집·처리 권한을 줄 뿐, 안전하게 보관할 의무를 면제하지 않는다. 유출 면책 약관은 약관규제법상 무효이며, 법원은 이런 면책 시도를 오히려 불리하게 본다.

**유효한 방어**: "법이 요구하는 모든 안전조치를 이행했다"를 증명하는 기술적 기록.

가장 강한 진술은 이것이다:
> "우리 직원도 그 데이터를 볼 수 없는 구조였습니다."

### D-010. 관리형 SFU로 시작 `[DECIDED]`

초기: LiveKit Cloud / Daily / Agora 등 관리형
이후: 트래픽 규모 확보 시 자체 운영(mediasoup, Janus)으로 이전 검토

**근거**: WebRTC는 1인 운영에 가장 가혹한 영역이다. 메시지는 5분 늦어도 되지만 통화는 5초 끊기면 사용자가 이탈한다. TURN/SFU 장애는 실시간으로 즉시 드러나 24시간 온콜을 요구한다. 초기 트래픽에서 관리형과 자체 운영의 절대 금액 차이는 작고, 그때 아낀 시간이 제품에 들어가는 편이 남는다.

### D-011. 1차 진입 플로우를 "아이디로 친구 추가" 하나로 단순화 `[DECIDED]`

세션(코드로 참여하는 임시 방)과 다이렉트(아이디로 연결)를 **동시에** 노출하던 것을,
**다이렉트 하나로** 줄인다. 세션은 UI에서 뺀다.

**근거**: §1.3의 "일회성·목적성 사용" 가설 자체는 유효하다. 문제는 두 개념을 같이 띄우니
첫 화면의 진입점이 3개가 됐다는 것이다(세션 생성 / 세션 참여 / 아이디로 시작).
가입 마찰을 줄이자던 §1.3이 오히려 **선택 마찰**을 만들었다.

1차 사용자는 친구다. 반복 사용이라 일회성 방이 필요 없다.
검증할 것도 "낯선 사람과 한 번 통화"가 아니라 "친구끼리 계속 쓸 만한가"다.

**§1.4(소셜 그래프를 키우지 않는다)와의 관계**: 충돌하지 않는다.
아이디로 1:1 연결만 만든다. 탐색·추천·피드·상호 친구 노출은 없다.
목록은 내가 직접 만든 연결의 나열일 뿐, 그래프를 **키우는** 기능이 아니다.

**되돌리는 비용**: 낮다. `sessions` 테이블, `lib/sessions.ts`, `is_channel_member()` 의
세션 분기를 전부 남겨뒀다. UI만 다시 붙이면 된다.

`[OPEN]` 세션(일회성 진입)은 §1.3 가설을 실제로 검증할 때 되살린다. 시점 미정.

### D-012. 아이디 변경 허용 `[DECIDED]`

당초 "닉네임은 한 번 정하면 변경 불가"에서 변경.

**근거**: 아이디가 바뀌면 상대가 못 찾는다는 우려였는데, 연결은 아이디가 아니라
`directs` 행(user id 기준)으로 유지되므로 **이미 맺은 관계는 안 끊긴다**.
반면 오타로 정한 아이디를 영영 못 고치는 것은 확실한 손해다. 후자가 더 크다.

---

---

## 3. 미해결 핵심 질문

> **이것들이 풀리기 전에는 사업 성립 여부를 판단할 수 없다.**

### Q-001. 무료 티어 경계선 `[OPEN]` ← **최우선**

크레딧이 유일한 수익원이고, 영상통화가 유일한 실질 변동비다. 질문은 하나로 좁혀진다:

> **무료 사용자의 영상통화 원가를 크레딧 매출이 덮을 수 있는가?**

필요한 미지수:
- `[UNKNOWN]` 크레딧 전환율 (무료 사용자 중 결제 비율)
- `[UNKNOWN]` 1인당 월평균 영상통화 시간
- `[UNKNOWN]` 녹화 사용 비율
- `[UNKNOWN]` 결제 사용자 1인당 월 지출

**주의**: 한번 열어준 무료 한도를 축소하는 것은 사용자 반발이 가장 큰 변경이다. **감으로 정하면 안 된다.**

초기 가설 (검증 대상):
- 텍스트 채팅: 무제한 무료 (원가 ≈ 0)
- 1:1 음성: 무제한 또는 넉넉한 한도
- 영상통화: 월 N시간 무료, 초과분 크레딧
- 녹화: 전량 크레딧

### Q-002. AI 마진율 `[OPEN]`

당초 구상: 원가 대비 +20%

**문제**: PG 수수료(약 3%), 부가세, 무료 사용자 트래픽, TURN 릴레이 대역폭, 스토리지 I/O를 빼면 실질 마진이 0 근처 또는 마이너스.

**더 근본적인 문제**: 통화·스토리지 원가를 AI 마진으로 회수할 수 없다. AI를 안 쓰고 영상통화만 쓰는 무료 사용자는 순손실이다.

`[OPEN]` 통상 AI 래퍼는 2~5배 마크업 또는 정액 구독 + 사용량 상한 하이브리드. 어느 쪽을 택할지 미정.

### Q-003. 선불 크레딧의 법적 형태 `[OPEN]` `[RISK]`

선불 충전 방식은 국내에서 **전자금융거래법상 선불전자지급수단** 이슈가 있다. 자가발행 범위를 넘으면 등록 의무, 미사용 잔액 환불 의무, 유효기간 규제가 따라온다.

**초기 권장**: 선불 충전보다 **후불 종량 또는 구독 + 초과 종량**이 법적·회계적으로 단순하다.

`[RISK]` 이 부분은 반드시 전문가 확인 필요. 이 문서의 법률 관련 서술은 전부 참고용이며 법률 자문이 아니다.

### Q-004. 녹화 방식 최종 결정 `[OPEN]`

D-004 참조. 로컬 / 서버 / 병행 중 미정.

### Q-005. E2EE 적용 범위와 시점 `[OPEN]`

일반 대화방에 E2EE를 언제 적용할 것인가. 1단계부터인가, 이후인가.

`[RISK]` E2EE를 표방한 뒤 나중에 예외를 만들면 신뢰가 무너진다. **할 거면 처음부터, 안 할 거면 처음부터 안 한다고 말할 것.**

---

## 4. 데이터 모델 및 보관 정책

### 4.1 보관 기간 요약

| 데이터 | 보관 | 비고 |
|---|---|---|
| 텍스트 메시지 | 14일 | D-001 |
| 첨부 파일/이미지 | 14일 | 메시지와 동일 수명 |
| 통화 녹화본 | 30분 | D-003, 옵트인 시에만 존재 |
| 통화 메타데이터(시각·참가자·시간) | `[OPEN]` | 과금 근거로 필요. 최소 필드만 |
| 계정 정보 | 계정 삭제 시까지 | 최소 수집 |
| 감사 로그 | `[OPEN]` | 법적 방어 목적. 내용은 미포함 |
| 결제/정산 기록 | 법정 보존기간 | 전자상거래법상 5년 등 `[UNKNOWN]` 정확한 적용 범위 |

### 4.2 방 타입 분리 `[FIXED]`

D-005에 따라 스키마 1단계부터 분리.

```
room.type:
  - 'standard'   → E2EE 대상 (또는 로드맵)
  - 'ai_enabled' → 서버가 평문 접근. UI에 명시 표시
```

**한번 ai_enabled로 만든 방은 standard로 되돌릴 수 없다.** (이미 평문이 서버를 거쳤으므로)

### 4.3 "진짜 삭제"의 요건 `[FIXED]` `[RISK]`

> **이것이 이 제품에서 가장 중요한 기술적 약속이며, 가장 깨지기 쉬운 부분이다.**

"2주 후 삭제"가 사실이 되려면 다음이 **전부** 지워져야 한다.

- [ ] Primary DB
- [ ] Read replica
- [ ] **DB 백업 / PITR (Point-in-Time Recovery)** ← 가장 흔한 함정
- [ ] Object storage 원본
- [ ] CDN 캐시
- [ ] 로그 / APM / 에러 트래킹 도구에 흘러들어간 페이로드
- [ ] 검색 인덱스 (도입 시)

`[RISK]` **Supabase 기본 설정은 이 요구와 충돌한다.** 유료 플랜의 PITR은 최대 수십 일치 백업을 보관하며, 여기엔 이미 "삭제된" 메시지가 그대로 남는다. **백업에 6개월치가 남아있으면 마케팅 문구가 거짓이 되고, 그것이 유출 자체보다 큰 리스크다.**

**대응 방향** `[OPEN]`:
1. 메시지 테이블만 백업 대상에서 제외 (별도 DB 인스턴스 분리)
2. 백업 보존 기간을 14일 이하로 강제
3. 메시지를 DB에 평문 저장하지 않고, 키를 별도 관리해 키 폐기로 무력화 (crypto-shredding)

→ **3번이 가장 강력하다.** 백업에 암호문이 남아도 키가 없으면 복호화 불가. 단 키 관리 복잡도가 올라간다.

### 4.4 TTL 구현 방식 `[DECIDED]`

**메시지 테이블은 날짜 기준 파티셔닝.** 삭제는 `DELETE` 가 아니라 **파티션 DROP**.

**근거**: 대량 DELETE는 vacuum 부담과 dead tuple을 남긴다. 파티션 드롭은 즉시적이고, 물리적 삭제에 훨씬 가깝다.

```
messages_2026_w32
messages_2026_w33  ← 매주 새 파티션 생성, 2주 지난 파티션 DROP
```

Supabase에서는 `pg_cron`으로 스케줄링. Storage 객체는 별도 cron job으로 정리 (**Supabase Storage에는 네이티브 TTL이 없음** `[RISK]`).

---

## 5. 아키텍처

### 5.1 스택 현황

- **확보**: Next.js + Supabase
- **방침**: 고도화 필요 시 스택 추가 도입 가능

### 5.2 컴포넌트 구성

```
┌─────────────────────────────────────────────────┐
│  Client (Next.js / React)                       │
│  - WebRTC PeerConnection                        │
│  - MediaRecorder (로컬 녹화 시)                  │
│  - E2EE 키 관리 (standard 룸)                    │
└────────┬──────────────────────┬─────────────────┘
         │                      │
    signaling                media
         │                      │
┌────────▼──────────┐   ┌───────▼─────────────────┐
│ Supabase Realtime │   │ SFU (관리형, D-010)      │
│ - broadcast       │   │ - 다자 통화              │
│ - presence        │   │ - 서버 녹화 (선택 시)     │
└────────┬──────────┘   └───────┬─────────────────┘
         │                      │
         │              ┌───────▼─────────────────┐
         │              │ TURN (coturn / 관리형)   │
         │              └─────────────────────────┘
         │
┌────────▼────────────────────────────────────────┐
│ Supabase                                         │
│ - Postgres (파티션 + pg_cron TTL)                │
│ - Auth                                           │
│ - Storage (녹화본, 30분 TTL)                     │
│ - Edge Functions (AI 프록시, 크레딧 차감)         │
└──────────────────────────────────────────────────┘
```

### 5.3 통화 토폴로지 `[DECIDED]`

| 상황 | 방식 | 서버 원가 |
|---|---|---|
| 1:1, 녹화 없음 | **P2P** (TURN fallback) | ≈ 0 (릴레이 시 제외) |
| 1:1, 서버 녹화 | SFU 경유 필수 | 실비 발생 |
| 다자 (3인+) | SFU | 실비 발생 |

**핵심**: 서버 녹화는 미디어가 서버를 통과해야만 가능하다. 즉 **녹화를 켜는 순간 P2P의 비용 이점이 사라진다.** 이것이 D-002/D-004가 중요한 이유.

### 5.4 Supabase / Vercel 제약 `[RISK]`

구현 전에 반드시 확인할 것:

- **Vercel serverless는 장기 연결을 유지할 수 없다.** 시그널링은 Supabase Realtime 또는 별도 서버로.
- **SFU는 Vercel/Supabase에서 돌릴 수 없다.** 별도 인프라 필수.
- **Supabase Storage에 네이티브 객체 TTL이 없다.** cron 직접 구현.
- **Realtime 동시 연결 수에 플랜별 상한이 있다.** `[UNKNOWN]` 현재 한도 확인 필요.
- **PITR 백업이 §4.3과 충돌한다.**

---

## 6. 미디어 파이프라인

### 6.1 원가 기준선 `[UNKNOWN]` (추정치, 검증 필요)

SFU 경유 1:1 영상통화 1시간, 1.5Mbps 기준:
- 서버 egress ≈ 1.3GB
- 클라우드 대역폭 $0.09/GB 가정 → **약 $0.12/시간**

**녹화 시 추가**: 컴포지팅 트랜스코딩은 스트림당 CPU 코어를 거의 하나씩 소비한다. **대역폭보다 트랜스코딩이 더 비싸다.**

`[UNKNOWN]` 실제 단가는 선택한 관리형 SFU의 분당 요금표로 재계산할 것. 위 숫자는 자체 운영 기준의 대략적 감각일 뿐이다.

### 6.2 무료 사용자 원가 문제 `[RISK]`

> 텍스트만 쓰는 무료 사용자는 원가가 0에 수렴하지만,
> **영상통화를 쓰는 무료 사용자는 시간당 원가가 선형으로 붙는다.**

"안 쓰면 그만"인 서비스에서 가장 위험한 상태는, 안 쓰는 사람은 매출 0이고 쓰는 사람은 원가만 쓰는 것. → Q-001로 연결.

---

## 7. 수익 모델

### 7.1 구조

```
수익 = 크레딧 매출 (+ 광고 ≈ 0)
비용 = 고정비(인프라 기본) + 변동비(영상통화·녹화·AI)
```

### 7.2 크레딧 대상 `[OPEN]`

과금 후보:
- AI 사용량 (토큰 기반)
- 영상통화 무료 한도 초과분
- 녹화 기능
- `[OPEN]` 그 외?

### 7.3 광고를 0으로 계산하는 근거 `[DECIDED]`

**단가**: 비타겟팅 광고 CPM은 타겟팅 대비 1/5~1/10. 그리고 **트래픽이 늘어도 CPM은 거의 오르지 않는다.** 광고주의 지불 의사는 규모가 아니라 전환율이 정하기 때문. 늘어나는 것은 총액이지 단가가 아니다.

**노출량**: 메신저는 대화 화면 체류형이라 온보딩/동의 화면 노출은 사용자당 한 자릿수.

**규모 감각** `[UNKNOWN]`:
```
MAU 10만 × 노출 3회 = 월 15만 노출
CPM $0.5 가정 → 월 매출 약 $75
```
→ 서버비 한 달치에도 못 미친다.

**결론**: 광고는 수익원이 아니라 **"우리는 광고 네트워크를 쓰지 않는다"를 보여주는 신호**로 취급한다. 손익 계산에서는 0으로 놓는다.

### 7.4 광고 지면의 대안 용도 `[OPEN]`

동의 화면 하단 슬롯을 외부 광고 대신 **자사 크레딧 상품 안내**에 쓰는 방안.
- 전환율이 훨씬 높음
- 외부 SDK 불필요 → 추적 문제 없음
- 포지셔닝 훼손 없음

어차피 광고 수익이 서버비 미만이라면 모든 면에서 유리하다.

---

## 8. 보안 및 법적 방어선

### 8.1 원칙 `[FIXED]`

D-009 재확인: **방어선은 동의서가 아니라 기술적 안전조치의 증명이다.**

### 8.2 감사 로그 구조 `[DECIDED]`

D-008로 외부 감사업체는 뺐지만, **증거 생성 구조는 유지한다.** 내부 운영만으로도 대부분의 가치가 나온다.

구현 요소:
- **Append-only 감사 로그**: 모든 관리자 접근, 삭제 작업, 키 사용 기록
- **해시 체인**: 각 로그 엔트리가 이전 엔트리의 해시를 포함 → 중간 삽입/수정 불가
- **외부 앵커링**: 일 1회 루트 해시를 외부에 타임스탬프 고정 → 사후 전체 재작성 불가
- **삭제 이행 증명**: 파티션 드롭 시각과 대상을 검증 가능한 형태로 기록

**이 구조의 가치는 책임 전가가 아니라 자기 방어다.** 과징금 산정에서 안전조치 의무 이행 여부는 크게 반영되며, 이를 증명하지 못해 무너지는 사례가 대부분이다.

`[OPEN]` 앵커링 대상 (공개 원장 / RFC 3161 TSA / 기타)

### 8.3 사람이 평문에 접근할 수 없는 구조 `[FIXED]`

- 관리자 계정으로도 메시지 평문 조회 불가
- 장애 대응이 필요한 경우: 이중 승인 + 전체 로그 기록 + 사후 통지
- 암호화 키와 데이터 저장소의 운영 주체 분리

### 8.4 실제 리스크 규모 인식 `[RISK]`

> **"피해가 작으니 배상이 작다"는 논리에 의존하지 말 것.**

- **법정손해배상**: 피해자가 손해액 입증 없이 1인당 최대 300만원 청구 가능
- **과징금**: 위반 관련 전체 매출액의 최대 3%
- **핵심**: 두 제도 모두 **유출된 데이터 양이 아니라 영향받은 사용자 수에 연동**된다.

즉 2주치만 털려도, 그 2주간 활성 사용자가 10만이면 청구인은 10만 명이다. **데이터 최소화가 배상 규모를 줄여주는 폭은 기대보다 작다.**

**징벌적 손해배상(최대 5배)은 고의·중과실 요건이라 성실 운영 시 적용 가능성이 낮다.** 실제 위험은 위 두 가지다.

**그리고 배상액보다 무서운 것**: 유출 사고를 낸 신생 메신저는 판결이 아니라 **다음 주 이탈률로 죽는다.** 카카오는 대안이 없어 살아남지만, 대체 가능한 서비스는 사고 하나로 사라진다.

→ **사고가 나면 끝난다는 전제로 설계할 것.** 이것이 이 포지션의 유일한 생존 조건이며, 다행히 마케팅 메시지와 같은 방향이다.

### 8.5 실무 준비사항 `[OPEN]`

- [ ] 사이버보험 (개인정보 유출 특약) — 스타트업 규모에서 연 수백만원대 가입 가능
- [ ] 유출 사고 시 **72시간 내 신고 의무** → 대응 절차 사전 문서화. 미이행 자체가 별도 위반
- [ ] 법인 분리를 통한 리스크 격리 검토
- [ ] **개인정보 전문 변호사 사전 상담** (필수)
- [ ] ISMS-P / SOC 2는 B2B 확장 시점에 검토. SOC 2 Type II는 준비 비용 수천만원대, 기간 6개월+ `[UNKNOWN]` 최신 시세 확인 필요

---

## 9. 로드맵

**원칙**: 녹화를 마지막에 둔다. 녹화는 앞의 모든 설계를 바꾸는 요구사항이라, 초반에 넣으면 나머지가 전부 인질이 된다.

| 단계 | 내용 | 검증할 것 |
|---|---|---|
| **1** | 1:1 텍스트 채팅 + TTL 파티셔닝 | 진짜 삭제가 실제로 되는가 (§4.3 체크리스트 전항목) |
| **2** | 1:1 WebRTC 음성/영상 (P2P + TURN) | 연결 성공률, TURN 릴레이 비율 |
| **3** | AI 챗봇 + 크레딧 과금 | Q-001 / Q-002 실측 |
| **4** | 다자 통화 (SFU 도입) | 원가 곡선 |
| **5** | 녹화 (동의 프로토콜 포함) | UX 마찰, 법적 검토 완료 후 |

**1단계에서 반드시 함께 할 것**:
- 방 타입 분리 (D-005) — 나중에 하면 전체 재설계
- 감사 로그 해시 체인 (§8.2) — 나중에 붙이면 과거 구간을 증명 못함
- E2EE 여부 결정 (Q-005) — 번복하면 신뢰 손상

---

## 10. 용어

| 용어 | 의미 |
|---|---|
| SFU | Selective Forwarding Unit. 미디어를 중계하는 서버. 다자 통화·서버 녹화에 필수 |
| TURN | NAT 통과 실패 시 미디어를 릴레이하는 서버. 대역폭 원가 발생 |
| PITR | Point-in-Time Recovery. DB를 과거 시점으로 복구하는 백업. §4.3의 함정 |
| Crypto-shredding | 데이터 대신 암호화 키를 폐기해 복호화 불가능하게 만드는 삭제 기법 |
| 앵커링 | 해시값을 외부 신뢰 지점에 고정해 사후 조작을 불가능하게 만드는 것 |

---

## 부록 A. 이 문서에 아직 없는 것

AI가 다음 라운드에서 채울 후보:

- [ ] DB 스키마 (테이블·컬럼·RLS 정책)
- [ ] 시그널링 메시지 포맷
- [ ] 크레딧 차감 로직 상세 (동시성, 잔액 부족 처리)
- [ ] AI 프록시 구조 (토큰 카운팅, 스트리밍, 원가 산정)
- [ ] E2EE 키 교환 프로토콜
- [ ] 온보딩 플로우 (동의 게이트 최소화 방안)
- [ ] 원가·손익 계산 모델 → **Q-001 해결에 필요**
- [ ] 장애 대응 런북 (1인 운영 전제)

## 부록 B. 이 문서의 법률 서술에 대한 고지

본 문서의 법률 관련 서술은 일반적 참고 정보이며 법률 자문이 아니다. 실제 사업 착수 전 개인정보보호 전문 변호사의 검토를 받을 것. 특히 D-002(녹화 동의), Q-003(선불 크레딧), §8.4(배상 규모)는 반드시 확인이 필요하다.
